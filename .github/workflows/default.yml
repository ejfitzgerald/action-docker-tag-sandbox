name: Build and push images

on: [push]

jobs:
  build-and-push:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Run tag
      - uses: ejfitzgerald/action-docker-tag@main
