name: Build and push images

on: [push]

jobs:
  build-and-push:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Run tag action
        uses: ejfitzgerald/action-docker-tag@01ab3d866b696ab7fd7b7cc082ea5a9cc3043e19
        with:
          repo: foobar
